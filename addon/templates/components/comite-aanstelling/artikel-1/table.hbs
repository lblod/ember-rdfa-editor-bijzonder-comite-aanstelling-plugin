<table class="data-table data-table--lined data-table--zebra" style="margin-bottom: 20rem;">
  <thead>
    <tr>
      <th>
          Officiële naam
      </th>
      <th>Status opname</th>
      <th>
        <div class="grid">
          <div class="col--3-12">
            Actie
          </div>
          <div class="col--9-12 u-align--right">
            {{#wu-link-button class="button--small" isNarrow=true onClick=(action "addMandataris")}}
              Voeg persoon toe
            {{/wu-link-button}}
          </div>
        </div>
      </th>
    </tr>
  </thead>
  <tbody>
    {{#if addMandatarisMode}}
      {{comite-aanstelling/artikel-1/table-row-add-mandataris
        onCancel=(action "cancelAddMandataris")
        onSave=(action "saveAddMandataris")
        bestuursorgaan=bestuursorgaan
      }}
    {{/if}}

    {{#each sortedMandatarissen as |mandataris|}}
      {{comite-aanstelling/artikel-1/table-row
         mandataris=mandataris
         mandatarissen=sortedMandatarissen
         onRemove=(action "remove")
         opvolgers=opvolgers
      }}
    {{/each}}
  </tbody>
</table>
{{yield}}
