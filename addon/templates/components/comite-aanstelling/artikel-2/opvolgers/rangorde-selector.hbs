{{#power-select
   disabled=disabled
   options=options
   selected=mandataris.opvolgerPlaats
   onchange=(action "select")
   renderInPlace=true
   allowClear=true
   as |name|}}
  {{name}}
{{/power-select}}
