<table class="data-table data-table--zebra">
  <thead>
    <tr>
      <th>
        Officiële naam
      </th>
      <th>Start</th>
      <th>Einde</th>
      <th>
        <div class="grid">
          <div class="col--3-12">
            Actie
          </div>
          <div class="col--9-12 u-align--right">
            {{#wu-link-button class="button--small" isNarrow=true onClick=(action "addMandataris")}}
              Voeg kandidaat toe
            {{/wu-link-button}}
          </div>
        </div>
      </th>
    </tr>
  </thead>
  <tbody>

    {{#if addMandatarisMode}}
      {{comite-aanstelling/artikel-2/add-mandataris
        onCancel=(action "cancelAddMandataris")
        onSave=(action "saveAddMandataris")
        bestuursorgaan=bestuursorgaan
        mandatarissen=mandatarissen
      }}
    {{/if}}

    {{#each sortedMandatarissen as |mandataris|}}
      {{comite-aanstelling/artikel-2/table-row mandataris=mandataris mandatarissen=mandatarissen}}
    {{/each}}
  </tbody>
</table>
